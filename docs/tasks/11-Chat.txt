```text
# 11-Chat — Personal AI Command Center Frontend
# Format: Hybrid (L1 registry + L3 detail blocks)
# Generated: 2026-04-24 | Unified from 11-Chat-Overview, 11-Chat-Modules, 11-Chat-Research
# Ready for cross-model deployment

════════════════════════════════════════════════════════════
## 📦 CHECKPOINT BLOCK (paste at session start)
════════════════════════════════════════════════════════════
version: "1.0"
module: "11-Chat"
last_updated: "2026-04-24"
completed_tasks: []
in_progress: null
next_task: "CHAT-001"
intent: "Implement all Chat module tasks following the registry below. Global contracts ($g, $c, $lc) are inherited from 01-Foundations and extended with chat-specific rules. Tasks use two‑tier subtasks: pipe (mechanical) and behavioral (code‑preserving)."
════════════════════════════════════════════════════════════

════════════════════════════════════════════════════════════
## 🌐 GLOBAL CONTRACT BLOCK (L0 + L2)
════════════════════════════════════════════════════════════
contracts:
  $g:  # Inherited from 01-Foundations (all 18 active); key ones for Chat:
    "1": "BackgroundColor #050507"
    "2": "Motion v12"
    "3": "@tanstack/react-virtual"
    "4": "oklch() tokens"
    "5": "Glass .noise-overlay"
    "6": "useReducedMotion()"
    "7": "AnimatePresence page transitions"
    "8": "Focus restoration"
    "10": "WCAG 2.2 AA"
    "11": "Test pattern: userEvent.setup(), screen"
    "12": "react-router imports"
    "13": "Motion tokens from lib/motion.ts"
    "15": "SanitizedHTML for untrusted HTML"
    "16": "No dangerouslySetInnerHTML outside SanitizedHTML"
    "17": "Offline-first Dexie CommandCenterDB"
    "18": "Storage quota monitoring"

  $c:  # Chat-specific cross-cutting contracts
    "CHAT-C01": "Message Cache SSOT: TanStack Query cache is the only source of truth for messages. No local useState message arrays."
    "CHAT-C02": "Optimistic Updates: User messages added via queryClient.setQueryData in onMutate. Rollback on error via snapshot. onSettled (not onSuccess) triggers invalidation."
    "CHAT-C03": "Stable React Keys: Optimistic messages use clientMsgId from crypto.randomUUID() once; never replaced with server ID."
    "CHAT-C04": "Streaming Transport: SSE over fetch + ReadableStream. Not EventSource. Not WebSockets."
    "CHAT-C05": "Stream Lifecycle: AbortController in useRef; abort on unmount and new stream; suppress AbortError."
    "CHAT-C06": "Scroll Contract: Auto-scroll to bottom only if user within 50px of bottom. Preserve position otherwise."
    "CHAT-C07": "Cache Config: staleTime: 0 + gcTime: Infinity for messages."
    "CHAT-C08": "ARIA Live Regions: Message container role='log' + aria-live='polite'. CheckpointBanner role='status' + aria-live='assertive'."
    "CHAT-C09": "Motion Guard: All animated components check useReducedMotion(). If true: skip character reveal, skip typing dots, skip spring transitions."
    "CHAT-C10": "Test Infrastructure: All network calls mocked via MSW; TanStack Query wrapped in createWrapper()."

  $lc:  # Local task-scoped contracts (reusable within Chat)
    scroll: "Auto-scroll: track userScrolled flag (distanceFromBottom > 50); scrollToIndex bottom on new message if not userScrolled; FAB appears when scrolled up"
    stream: "SSE stream: buffer partial chunks; parse named events; reconnect 3x with backoff; abort on unmount; suppress AbortError"
    optimsg: "Optimistic message: onMutate append with clientMsgId + status:'sending'; onError rollback; onSettled invalidate; key=clientMsgId permanent"
    sandbox: "Artifact sandbox: iframe sandbox='allow-scripts' only; Blob URL for origin; CSP blocks external scripts/fetch; DOMPurify all code before injection"
    a2anode: "React Flow node: custom nodes must export <Handle>; use Dagre for auto-layout; cleanup Dagre graph on node delete"
    mcpconsent: "MCP tools: explicit user consent required for write operations; never auto-execute without approval"
    knowquota: "Knowledge base: monitor navigator.storage.estimate(); warn at 80% usage; chunk files at 10MB for writes"

  motion:  # Chat motion tier
    ThreadList_items: "Quiet — opacity fade on mount, 150ms max, no stagger"
    MessageBubble_user: "Static — instant render"
    MessageBubble_agent_new: "Alive — opacity:0→1, y:10→0, spring stiffness:300 damping:30"
    Streaming_text: "Alive — character reveal ~20ms/char; blinking | cursor; suppressed when useReducedMotion()"
    Typing_indicator: "Alive — three-dot keyframes opacity [0.3,1,0.3], staggered delays"
    LED_input_border: "Alive — boxShadow glow on focus; 100ms brightness flash on keypress"
    ToolCallDisclosure_chevron: "Alive — rotate: isOpen ? 180 : 0, spring"
    CheckpointBanner: "Alive — y: -20→0 with AnimatePresence"
    Slash_command_menu: "Quiet — opacity fade 100ms"
    Canvas_pane_resize: "Quiet — opacity on hover, 150ms"
    Artifact_iframe_load: "Alive — opacity:0→1, scale:0.98→1, spring"
    Snapshot_list_item: "Quiet — opacity fade 150ms"
    MCP_tool_status_dot: "Alive — pulse keyframes (green/amber/red)"
    Agent_node_drag_overlay: "Alive — glowing, scaled card with spring follow"

  anti-patterns:  # Global severity (inherited)
    S: "Security — keep verbatim"
    P: "Performance — keep verbatim"
    B: "Implementation bug — keep verbatim"
    ST: "Style preference — safe to compress"

════════════════════════════════════════════════════════════

════════════════════════════════════════════════════════════
## L1 — MODULE REGISTRY (all 22 Chat tasks)
════════════════════════════════════════════════════════════
id       |stat|pri|eff |depends               |files (selected)                                            |$cut
CHAT-001 |P   |H  |1.0 |FND-007,FND-008       |pages/ChatPage.tsx, components/chat/ChatLayout.tsx, router/routes.ts|$g7,$g8
CHAT-002 |P   |H  |2.5 |FND-006              |queries/chat.ts, hooks/useChatReducer.ts, hooks/useChatMessages.ts, mocks/handlers/chat.ts|$c:CHAT-C01,$c:CHAT-C02,$c:CHAT-C03,$c:CHAT-C07,$c:CHAT-C10
CHAT-003 |P   |H  |1.5 |CHAT-002             |components/chat/ThreadList.tsx, ThreadListItem.tsx         |$c:CHAT-C06
CHAT-004 |P   |H  |2.0 |CHAT-002             |components/chat/MessageList.tsx, MessageBubble.tsx         |$c:CHAT-C08,$c:CHAT-C09
CHAT-005 |P   |H  |2.0 |CHAT-004             |hooks/useChatScroll.ts, components/chat/MessageList.tsx    |$c:CHAT-C06,$lc:scroll
CHAT-006 |P   |H  |2.0 |CHAT-002             |components/chat/ChatInput.tsx, TextareaAutosize            |$g4
CHAT-007 |P   |H  |2.5 |CHAT-002             |hooks/useSSEStream.ts, mocks/handlers/chat.ts             |$c:CHAT-C04,$c:CHAT-C05,$lc:stream
CHAT-008 |P   |M  |1.5 |CHAT-006             |components/chat/SlashMenu.tsx, data/slashCommands.ts      |$c:CHAT-C09
CHAT-009 |P   |M  |1.0 |CHAT-004             |components/chat/ToolCallDisclosure.tsx                      |—
CHAT-010 |P   |M  |1.0 |CHAT-002             |components/chat/CheckpointBanner.tsx                       |$c:CHAT-C08,$c:CHAT-C09
CHAT-011 |P   |H  |10.0|CHAT-001, CHAT-009    |components/chat/CollaborationPane.tsx, DiffPreview.tsx, CanvasEditor.tsx, hooks/useCanvasState.ts, stores/canvasStore.ts|$lc:sandbox,$g15
CHAT-012 |P   |H  |10.0|CHAT-011             |components/chat/ArtifactPreview.tsx, ArtifactSandbox.tsx, lib/artifactCompiler.ts, lib/sandboxTemplate.ts|$lc:sandbox,$g15
CHAT-013 |P   |M  |4.0 |CHAT-011             |stores/canvasStore.ts (extend), components/chat/SnapshotList.tsx, hooks/useCanvasSnapshots.ts|—
CHAT-014 |P   |M  |4.0 |CHAT-006             |components/chat/AttachmentZone.tsx, ImagePreview.tsx, hooks/useFileUpload.ts|—
CHAT-015 |P   |H  |6.0 |CHAT-001, FND-005, FND-006|components/chat/MCPSettingsPanel.tsx, MCPServerCard.tsx, ToolRegistryList.tsx, stores/mcpStore.ts, hooks/useMCPTools.ts|$lc:mcpconsent
CHAT-016 |P   |M  |5.0 |CHAT-015             |components/chat/AgentTerminal.tsx, TerminalEntry.tsx, hooks/useAgentActivity.ts|—
CHAT-017 |P   |M  |3.0 |CHAT-008, CHAT-015    |components/chat/SlashMenu.tsx (extend), data/slashCommands.ts (extend)|—
CHAT-018 |P   |H  |14.0|CHAT-001, CHAT-002, FND-004, FND-005|components/knowledge/KnowledgeBasePanel.tsx, FileUploadZone.tsx, DocumentList.tsx, lib/embeddingEngine.ts, lib/vectorStore.ts, lib/textChunker.ts, hooks/useKnowledgeBase.ts, stores/knowledgeStore.ts|$lc:sandbox,$lc:knowquota,$g15,$g17
CHAT-019 |P   |M  |5.0 |CHAT-018, CHAT-002    |components/memory/MemoryPanel.tsx, MemoryEntryCard.tsx, hooks/useMemory.ts, stores/memoryStore.ts|$lc:knowquota
CHAT-020 |P   |M  |6.0 |DASH-006, CHAT-018    |components/agents/AgentStudio.tsx, AgentBuilderForm.tsx, AgentTestChat.tsx, stores/agentStore.ts|—
CHAT-021 |P   |H  |14.0|CHAT-020, DASH-006    |components/orchestrator/A2AFlowEditor.tsx, AgentNode.tsx, FlowToolbar.tsx, FlowRunPanel.tsx, stores/flowStore.ts, hooks/useAgentFlow.ts|$lc:a2anode
CHAT-022 |P   |M  |3.0 |CHAT-006, CHAT-008    |pages/PromptLibraryPage.tsx, components/chat/PromptLibraryModal.tsx, PromptCard.tsx, stores/promptStore.ts, lib/mockData/prompts.ts|—

════════════════════════════════════════════════════════════

## L3 — TASK DETAIL BLOCKS
════════════════════════════════════════════════════════════

### sec-CHAT-001 — Page Layout & Route Configuration
effort|pri|deps|$cut
1.0|H|FND-007,FND-008|$g7,$g8

sub|type|description
CHAT-001A|pipe|Create `src/pages/ChatPage.tsx`: two-column layout (left thread list 240px fixed, right chat area flex-1). Use `<ChatLayout>` with slots. Wrap in `<motion.div>` page transition: `initial={{ opacity:0, y:8 }}` → `animate={{ opacity:1, y:0 }}` → `exit={{ opacity:0, y:-8 }}`, 200ms.
CHAT-001B|pipe|Responsive: on `<768px`, thread list moves into shadcn `Sheet` triggered by hamburger button. Chat area full width.
CHAT-001C|pipe|Route config in `src/router/routes.ts`: `path: 'chat'`, `lazy: () => import('@/pages/ChatPage')`, `loader: () => queryClient.ensureQueryData(chatThreadsQueryOptions)`.
CHAT-001D|behavioral|[TEST] Page renders both ThreadList and chat area; at 767px ThreadList not in flow but Sheet trigger present; route lazy‑loads and prefetches threads.

dod|criteria
D1|Two-column layout on desktop, responsive Sheet on mobile
D2|Page transition active; data prefetched by loader
D3|All tests pass

---

### sec-CHAT-002 — Chat State Management (Query Config, Reducer, MSW)
effort|pri|deps|$cut
2.5|H|FND-006|$c:CHAT-C01,$c:CHAT-C02,$c:CHAT-C03,$c:CHAT-C07,$c:CHAT-C10

sub|type|description
CHAT-002A|pipe|Create `src/queries/chat.ts` with Query Key Factory: `chatKeys.all`, `chatKeys.threads()`, `chatKeys.thread(id)`, `chatKeys.messages(threadId)`.
CHAT-002B|pipe|Define `messagesQueryOptions(threadId)`: `staleTime:0`, `gcTime:Infinity`.
CHAT-002C|behavioral|Define `useSendMessage(threadId)` mutation **using** the shared `useOptimisticMutation()` wrapper. `optimisticUpdate`: append user message with `clientMsgId: crypto.randomUUID()`, `status:'sending'`. Key carried permanently.
CHAT-002D|pipe|Define `useCreateThread()` and `useThreads()` with `chatKeys.threads()`.
CHAT-002E|pipe|Create `src/hooks/useChatReducer.ts` with pure reducer: state shape `{ input, isStreaming, streamingMessageId, showTypingIndicator, selectedModel, showSlashMenu, slashQuery }` and actions `SET_INPUT, START_STREAMING, APPEND_TOKEN, END_STREAMING, SET_MODEL, OPEN_SLASH_MENU, CLOSE_SLASH_MENU, SET_SLASH_QUERY, RESET_INPUT`.
CHAT-002F|pipe|Create `useChatMessages` hook composing `useQuery`, `useChatReducer`, and `useSendMessage`.
CHAT-002G|pipe|Create `src/mocks/handlers/chat.ts`: MSW handlers for `GET /api/threads`, `GET /api/threads/:id/messages`, `POST /api/threads/:id/messages` (echoes back with server ID + clientMsgId), `GET /api/threads/:id/stream` (SSE simulation).
CHAT-002H|pipe|Create `src/test/utils/createWrapper.tsx` for `renderHook` tests: fresh `QueryClient` with `retry:false`.
CHAT-002I|behavioral|[TEST] Key factory stable; query options have correct config; optimistic message appears instantly and rolls back on error; reducer transitions pure; MSW handlers return correct data.

dod|criteria
D1|`chatKeys` typed; `messagesQueryOptions` uses `staleTime:0, gcTime:Infinity`
D2|Optimistic mutation via wrapper; `clientMsgId` permanent key
D3|Reducer covers all UI states; `useChatMessages` composes correctly
D4|MSW handlers cover all endpoints; `createWrapper` available
D5|All tests pass

anti|description
A1|`Date.now()` as temp ID — use `crypto.randomUUID()`
A2|Replacing React key when server ID arrives
A3|`gcTime` default with `staleTime:0` — messages evicted mid-conversation
A4|`useState` for streaming/model/slash state — use reducer
A5|`setTimeout` for search debounce — use `useTransition`

---

### sec-CHAT-003 — Thread List
effort|pri|deps|$cut
1.5|H|CHAT-002|$c:CHAT-C06

sub|type|description
CHAT-003A|pipe|Build `ThreadList`: search input, "New Chat" button, scrollable list, empty state. No virtualization (≤100 items).
CHAT-003B|pipe|Search with `useTransition`: immediate input update, `startTransition` for filtering, `isPending` reduces list opacity.
CHAT-003C|pipe|`ThreadListItem`: title, relative timestamp (`date-fns/formatDistanceToNow`), first-line preview, agent badge. Active: left blue border. Click navigates via search params.
CHAT-003D|pipe|"New Chat": `useCreateThread()` optimistic prepend, navigate on success, rollback on error.
CHAT-003E|behavioral|[TEST] List renders; search non‑blocking; active state visual; new chat optimistic with rollback; no virtualization overhead.

dod|criteria
D1|Search responsive; thread navigation via search params
D2|Optimistic thread creation with rollback
D3|All tests pass

---

### sec-CHAT-004 — Message Display (Bubble & List)
effort|pri|deps|$cut
2.0|H|CHAT-002|$c:CHAT-C08,$c:CHAT-C09

sub|type|description
CHAT-004A|pipe|Build `MessageBubble` wrapped in `React.memo` with custom comparator. User: right-aligned charcoal `rounded-[18px_18px_4px_18px]`. Agent: left-aligned glass `rounded-[4px_18px_18px_18px]`. `aria-label` and `role="article"`.
CHAT-004B|pipe|Streaming text: `displayedContent` in `useRef`, reveal ~20ms/char, blinking cursor. `useReducedMotion()` → show full content instantly.
CHAT-004C|pipe|Entrance animation for new agent messages only: `opacity:0, y:10 → 1, 0` spring. Skip if reduced motion.
CHAT-004D|pipe|Message status on user messages: sending (amber pulse), sent (checkmark), error (red icon + Retry).
CHAT-004E|pipe|`MessageList`: renders bubbles keyed by `clientMsgId`, `TypingIndicator` when active, date separators. Container `role="log"` + `aria-live="polite"`.
CHAT-004F|pipe|Typing indicator: three dots `motion.span` staggered opacity keyframes. Reduced motion → static "…".
CHAT-004G|behavioral|[TEST] Styling correct; memo prevents re‑renders; streaming reveal works; entrance only on new messages; status/retry; ARIA roles present; typing animation respects motion preference.

dod|criteria
D1|User and agent bubbles styled; React.memo effective
D2|Streaming reveal and cursor; reduced motion respected
D3|Entrance animation only for new agent messages
D4|Message status with retry; typing indicator
D5|ARIA live region and date separators
D6|All tests pass

anti|description
A1|Keying by server ID — use permanent `clientMsgId`
A2|`useState` for `displayedContent` — use `useRef`+direct DOM
A3|Animating historical messages
A4|Missing `React.memo`

---

### sec-CHAT-005 — Virtual Scroll & Scroll Behavior
effort|pri|deps|$cut
2.0|H|CHAT-004|$c:CHAT-C06,$lc:scroll

sub|type|description
CHAT-005A|pipe|Install `@tanstack/react-virtual@3.13.24`
CHAT-005B|pipe|Integrate `useVirtualizer` in `MessageList`: `count=messages.length`, `estimateSize:()=>80`, `measureElement`, `overscan:5`. Render only virtual items.
CHAT-005C|pipe|Create `useChatScroll` hook: auto‑scroll bottom only if user within 50px; `userScrolled` flag; `scrollToBottom` resets flag; send message always triggers scroll.
CHAT-005D|pipe|"Scroll to bottom" FAB with `AnimatePresence` when `userScrolled`.
CHAT-005E|pipe|Load‑more on scroll‑to‑top: fetch previous page, preserve viewport via scroll anchoring.
CHAT-005F|behavioral|[TEST] Only visible rows in DOM; auto‑scroll conditional; FAB appears and functions; load‑more preserves position.

dod|criteria
D1|Virtualization bounds DOM nodes
D2|Auto‑scroll respects user position; FAB visible
D3|Load‑more without viewport jump
D4|All tests pass

---

### sec-CHAT-006 — Chat Input (Textarea, LED Border, Send)
effort|pri|deps|$cut
2.0|H|CHAT-002|$g4

sub|type|description
CHAT-006A|pipe|Install `react-textarea-autosize`
CHAT-006B|pipe|`ChatInput`: `<TextareaAutosize minRows=1 maxRows=5 cacheMeasurements>`, controlled via reducer. Send button disabled when empty or streaming. Model selector dropdown. Attach placeholder button.
CHAT-006C|behavioral|LED border: focus glow (`box-shadow` electric blue), keypress flash 100ms (add/remove class). CSS `oklch()`.
CHAT-006D|pipe|Send: `Enter` sends (call `sendMessage`, `RESET_INPUT`, `scrollToBottom`). `Shift+Enter` newline. `Cmd+Enter` sends. Streaming: show Stop button.
CHAT-006E|behavioral|[TEST] Textarea grows; send disabled checks; LED focus/flash; key combos work; Stop button aborts.

dod|criteria
D1|Auto‑resize with `cacheMeasurements`
D2|LED border effect (focus and keypress)
D3|Correct send/newline/stop behavior
D4|All tests pass

---

### sec-CHAT-007 — SSE Streaming Hook
effort|pri|deps|$cut
2.5|H|CHAT-002|$c:CHAT-C04,$c:CHAT-C05,$lc:stream

sub|type|description
CHAT-007A|pipe|Create `useSSEStream`: `AbortController` in `useRef`; `startStream(threadId)`, `abortStream()`. Uses `fetch` + `ReadableStream`. Abort on unmount and on new stream. Suppress `AbortError`. Reconnect 3x with backoff (1s,2s,4s) on network error. `Last-Event-ID` header.
CHAT-007B|pipe|Parse SSE: `TextDecoder`, buffer partial chunks, split by `\n`, handle multi-line data, named events.
CHAT-007C|pipe|Integrate: on send dispatch `START_STREAMING`; each `onToken` `setQueryData` appends token to cache; `onComplete` dispatch `END_STREAMING`, invalidate; `onError` set message status `'error'`.
CHAT-007D|pipe|Mock SSE in MSW: `ReadableStream` delivering tokens at intervals.
CHAT-007E|behavioral|[TEST] Stream lifecycle aborts correctly; tokens accumulate; completion invalidates; error handled; partial chunks parsed; reconnect with backoff.

dod|criteria
D1|`useSSEStream` manages lifecycle; abort on unmount & new stream
D2|SSE parsing robust; `Last-Event-ID` on reconnect; 3 retries
D3|Tokens update cache; completion invalidates
D4|All tests pass

anti|description
A1|`EventSource` — use `fetch`+`ReadableStream` for custom headers
A2|Not aborting previous stream before starting new
A3|`AbortError` calling `onError`
A4|Not buffering partial chunks

---

### sec-CHAT-008 — Slash Command Menu
effort|pri|deps|$cut
1.5|M|CHAT-006|$c:CHAT-C09

sub|type|description
CHAT-008A|pipe|Define `SLASH_COMMANDS` in `src/data/slashCommands.ts`: id, label, description.
CHAT-008B|pipe|`SlashMenu`: `AnimatePresence` fade; `position:absolute bottom-full`. Filtered list. `role="listbox"`, `role="option"` with `aria-selected`.
CHAT-008C|pipe|Trigger: `/` on empty input opens menu; typing filters; Escape closes; ArrowUp/Down move highlight; Enter selects command.
CHAT-008D|pipe|Close on outside click via `mousedown` listener.
CHAT-008E|behavioral|[TEST] Menu opens/closes correctly; keyboard nav; ARIA; outside click; animation present.

dod|criteria
D1|Slash menu opens, filters, keyboard navigates
D2|ARIA roles correct; outside click closes
D3|All tests pass

---

### sec-CHAT-009 — ToolCallDisclosure
effort|pri|deps|$cut
1.0|M|CHAT-004|—

sub|type|description
CHAT-009A|pipe|`ToolCallDisclosure`: shadcn `Collapsible`; trigger shows tool name + duration badge + chevron. Expanded: input/output JSON (read‑only `<pre>`). `role="group"`.
CHAT-009B|pipe|Chevron animation: `motion.div` spring rotate 0/180.
CHAT-009C|pipe|In `MessageBubble`, render disclosure for each `toolCall` in agent message. Re‑measure virtual row on expand.
CHAT-009D|behavioral|[TEST] Disclosure expands/collapses; JSON displayed; chevron rotates; virtual list re‑measures.

dod|criteria
D1|Disclosure renders tool info; expandable
D2|Chevron spring animation
D3|Virtual row height updates on expand
D4|All tests pass

---

### sec-CHAT-010 — CheckpointBanner
effort|pri|deps|$cut
1.0|M|CHAT-002|$c:CHAT-C08,$c:CHAT-C09

sub|type|description
CHAT-010A|pipe|`CheckpointBanner`: amber theme, warning icon, description, Approve/Reject buttons. `role="status"`, `aria-live="assertive"`.
CHAT-010B|pipe|`AnimatePresence` with height animation: `initial: height:0, opacity:0, y:-20`, spring. `layout` prop on `MessageList` wrapper.
CHAT-010C|pipe|Optimistic approve/reject mutations: clear checkpoint immediately, rollback on error.
CHAT-010D|behavioral|[TEST] Banner appears/disappears with animation; approve/reject optimistic; rollback works; ARIA assertive.

dod|criteria
D1|Spring slide-in/out with height animation
D2|Optimistic dismiss with rollback
D3|`aria-live="assertive"` announces immediately
D4|All tests pass

---

### sec-CHAT-011 — Collaboration Canvas (Split-Pane Editor, Diff & Apply)
effort|pri|deps|$cut
10.0|H|CHAT-001,CHAT-009|$lc:sandbox,$g15

sub|type|description
CHAT-011A|pipe|Install `@monaco-editor/react`, `react-resizable-panels`, `react-diff-viewer`, `vite-plugin-monaco-editor-esm`. Configure Vite plugin.
CHAT-011B|pipe|Create `src/stores/canvasStore.ts`: `isOpen, activeFileId, files Map, originalContent, currentContent, diffView split/unified, mode text/code, language`. Actions: `openCanvas, closeCanvas, setContent, applyEdit, rejectEdit`.
CHAT-011C|pipe|Build `CollaborationPane.tsx`: `react-resizable-panels` `PanelGroup direction="horizontal"`. Left: chat. Right: Canvas (collapsible, min 320px). Resize handle with electric blue line. Mobile: full‑screen Sheet.
CHAT-011D|pipe|Build `CanvasEditor.tsx`: `<MonacoEditor>` for code (minimap disabled), markdown for text. Read‑only by default; double‑click to edit. Amber pulsing dot when AI edit in progress.
CHAT-011E|behavioral|Build `DiffPreview.tsx`: shows when `originalContent !== currentContent`. Monaco `DiffEditor` for code, `react-diff-viewer` for text. Action bar: Apply (electric blue, glow hover) and Reject. Apply → `setQueryData` optimistically. `AnimatePresence mode="popLayout"` for diff transitions.
CHAT-011F|pipe|Wire into ChatInterface: agent messages with `type:'canvas_edit'` open Canvas; highlighted text sent with prompt; "Edit in Canvas" button on relevant messages.
CHAT-011G|pipe|Error boundary: `react-error-boundary` with `CanvasErrorFallback`; store unsaved content before crash.
CHAT-011H|behavioral|[TEST] Canvas opens; diff preview shows; Apply/Reject work; resize handle functional; error boundary catches Monaco crash; mobile Sheet fallback.

dod|criteria
D1|Split-pane with Monaco for code, markdown for text
D2|Diff preview side‑by‑side with line‑level highlighting
D3|Apply/Reject optimistically update
D4|Error boundary prevents chat crash
D5|All tests pass

anti|description (from modules doc)
A1|Forgetting `vite-plugin-monaco-editor-esm` — workers fail silently
A2|Using `srcdoc` for previews — use Blob URL+iframe.src for security
A3|Not storing unsaved content before error boundary triggers
A4|Rendering diff for every message — only when content differs

---

### sec-CHAT-012 — Live Artifacts (Sandboxed React/SVG Rendering)
effort|pri|deps|$cut
10.0|H|CHAT-011|$lc:sandbox,$g15

sub|type|description
CHAT-012A|pipe|Create `src/lib/sandboxTemplate.ts`: generate HTML doc with import map for React, ReactDOM, Tailwind (via CDN), Lucide, Radix. Apply CSP meta tag: `default-src 'self'; script-src 'self' https://esm.sh https://cdn.jsdelivr.net; connect-src 'none';`. Environment purification: freeze `Object.prototype`, proxy `window.open`.
CHAT-012B|pipe|Create `src/lib/artifactCompiler.ts`: accept raw code, transform with `@babel/standalone` (`presets:['react','typescript']`), extract default export, wrap in `ReactDOM.createRoot`. Sanitize with DOMPurify.
CHAT-012C|behavioral|Build `ArtifactSandbox.tsx`: `<iframe>` with `sandbox="allow-scripts"` only. `src` = `URL.createObjectURL(new Blob([htmlDoc],...))` — NOT `srcdoc`. Revoke previous Blob URL. Listen for `postMessage` for console/errors. Loading/error states. Entrance animation: `opacity:0→1, scale:0.98→1`, spring.
CHAT-012D|pipe|Build `ArtifactPreview.tsx`: toolbar with "Editor" / "Preview" / "Split" toggle. Split uses nested `react-resizable-panels`. Refresh, "Open in new tab", copy code buttons. For SVG/markdown, render directly with DOMPurify.
CHAT-012E|pipe|Wire agent-generated artifacts: `message.type === 'artifact'` or `application/vnd.ant.react` triggers preview. Tool calls that produce code auto‑trigger.
CHAT-012F|pipe|Security hardening: DOMPurify all code; validate import whitelist; monitor Blob URL memory; `X-Content-Type-Options: nosniff`.
CHAT-012G|behavioral|[TEST] Sandboxed iframe renders React component; `postMessage` captures console; error state displays on broken code; Blob URL revoked; DOMPurify strips malicious scripts.

dod|criteria
D1|Sandboxed iframe renders AI‑generated React, SVG, HTML, Markdown
D2|CSP + sandbox attributes provide defense‑in‑depth
D3|`@babel/standalone` transpiles JSX in‑browser
D4|Split view (Editor + Preview) works within Canvas
D5|All code sanitized via DOMPurify before injection
D6|All tests pass

anti|description
A1|Using `srcdoc` — inherits parent origin; use Blob URL
A2|Adding `allow-same-origin` to sandbox
A3|Forgetting `URL.revokeObjectURL()` — memory leak
A4|Allowing external `fetch()` in CSP — data exfiltration

---

### sec-CHAT-013 — Canvas Version Snapshots & Rollback
effort|pri|deps|$cut
4.0|M|CHAT-011|—

sub|type|description
CHAT-013A|pipe|Extend `canvasStore` with snapshot history: `snapshots[]`, `MAX_SNAPSHOTS=50` ring buffer, `createSnapshot(description?)`, `restoreSnapshot(id)`, `clearSnapshots()`. Auto‑create in `applyEdit`. Deep clone with `structuredClone`.
CHAT-013B|pipe|Build `SnapshotList.tsx`: vertical timeline sidebar (collapsible), relative timestamps, preview 80 chars, restore button on hover, current active highlighted with `layoutId` pill. Empty state.
CHAT-013C|pipe|Restore flow: confirmation dialog, save current as snapshot first (undoable restore), flash animation on content.
CHAT-013D|pipe|Integration with DiffPreview: snapshot vs current side‑by‑side.
CHAT-013E|behavioral|[TEST] Snapshot auto‑created on apply; restore reverts content; timeline displays chronologically; restore creates undo snapshot.

dod|criteria
D1|Auto‑snapshots before AI edits; manual creation possible
D2|Ring buffer limits to 50
D3|Restore undoable (creates new snapshot before restore)
D4|All tests pass

anti|description
A1|Not deep‑cloning — mutations corrupt history
A2|`JSON.parse(JSON.stringify())` for `structuredClone`‑able objects
A3|Restoring without saving current state first

---

### sec-CHAT-014 — Media Attachments (File Upload, Image Preview, Resize)
effort|pri|deps|$cut
4.0|M|CHAT-006|—

sub|type|description
CHAT-014A|pipe|Install `react-dropzone`, `jimp`.
CHAT-014B|pipe|Build `AttachmentZone.tsx`: `useDropzone` with accept (images, docs, code). Drag‑over dashed electric blue border. Max 25MB/file, 5 files/msg. Error toast on reject.
CHAT-014C|pipe|Build `ImagePreview.tsx`: thumbnail grid below input; `URL.createObjectURL` previews; lightbox on click; remove button revokes URL.
CHAT-014D|pipe|Integrate into `ChatInput`: replace placeholder attach button with functional `AttachmentZone`. File chips animate in with spring stagger. Serialize as `{ name, type, size, dataUrl }`.
CHAT-014E|pipe|Optional resizing: if >2MB, auto‑resize with Jimp (max 2048px, `RESIZE_BILINEAR`). Show "Resizing..." indicator.
CHAT-014F|behavioral|[TEST] Drag‑and‑drop adds file; invalid files rejected; preview renders; remove clears; files in message payload; Jimp resizes.

dod|criteria
D1|Drag‑and‑drop with validation and preview
D2|File chips animate in; optional client‑side resize
D3|Object URLs revoked on unmount
D4|All tests pass

anti|description
A1|Not revoking `URL.createObjectURL` — memory leak
A2|Blocking UI during image processing

---

### sec-CHAT-015 — MCP Integration (Settings UI & Tool Registry)
effort|pri|deps|$cut
6.0|H|CHAT-001,FND-005,FND-006|$lc:mcpconsent

sub|type|description
CHAT-015A|pipe|Create `src/stores/mcpStore.ts`: `servers[], connectedTools[], isConnecting, activeServerId`. Actions: `addServer, removeServer, toggleServer, testConnection, refreshTools`.
CHAT-015B|pipe|Create `useMCPTools` hook (mock): returns tools grouped by server. Cache `staleTime:5 min`. Mock: GitHub (create_issue, list_prs...), Slack, Postgres, Filesystem.
CHAT-015C|pipe|Build `MCPServerCard.tsx`: name, status dot (green/amber/red/gray pulse), tool count badge, toggle, Configure/Test/Remove buttons.
CHAT-015D|pipe|Build `MCPSettingsPanel.tsx`: full‑page or modal; "Add Server" button; server list/grid; empty state; pre‑built templates.
CHAT-015E|pipe|Build `ToolRegistryList.tsx`: expandable tree grouped by server; search/filter; each tool shows input schema preview; "Test" mini‑playground.
CHAT-015F|pipe|Wire into Chat: tool availability badge in `ChatInput`; tool calls displayed in `ToolCallDisclosure`.
CHAT-015G|behavioral|[TEST] Server card status dot; toggle enable/disable; test connection toast; tool list filters; add server form validates; empty state.

dod|criteria
D1|MCP server CRUD UI with configuration modals
D2|Tool registry searchable and grouped
D3|Connection status indicators animated
D4|Integration with Chat and ToolCallDisclosure
D5|All data mocked; real connectivity deferred
D6|All tests pass

anti|description
A1|Building real MCP stdio client in browser
A2|Storing API keys in plain text
A3|Auto‑executing tools without consent

---

### sec-CHAT-016 — Agent Terminal & Activity Replay
effort|pri|deps|$cut
5.0|M|CHAT-015|—

sub|type|description
CHAT-016A|pipe|Build `AgentTerminal.tsx`: collapsible panel, dark theme monospace, filter dropdown (All/Tool Calls/Errors/System), auto‑scroll bottom, virtualize with `@tanstack/react-virtual` for >50 entries.
CHAT-016B|pipe|Build `TerminalEntry.tsx`: `[timestamp] [agent-icon] [server] $ {tool_name}`; expandable input/output JSON; status indicators (✓/✗/⏳); duration badge.
CHAT-016C|pipe|Create `useAgentActivity` hook (mock): chronological activity log, filterable.
CHAT-016D|pipe|Wire into Dashboard ActivityFeed: "Open in Terminal" button on entries.
CHAT-016E|behavioral|[TEST] Terminal renders mock entries; expand shows I/O; filter works; virtualization handles 100+ entries; status indicators animate.

dod|criteria
D1|Terminal panel with dark theme, monospace
D2|Chronological log of tool calls with status
D3|Expandable entries; filterable; virtualized
D4|Integration with Dashboard ActivityFeed
D5|All tests pass

---

### sec-CHAT-017 — Slash Command Toolbar (MCP Tool Discovery)
effort|pri|deps|$cut
3.0|M|CHAT-008,CHAT-015|—

sub|type|description
CHAT-017A|pipe|Extend `slashCommands.ts`: add `source:'built-in'|'mcp'` field.
CHAT-017B|pipe|Extend `SlashMenu.tsx`: merge built‑in + MCP tools; group by source; show server badge with distinct color; keyboard nav across groups.
CHAT-017C|pipe|Selecting MCP tool inserts `/{tool.name} ` with input schema as placeholder.
CHAT-017D|behavioral|[TEST] Menu shows built‑in + MCP tools grouped; selecting inserts placeholder; keyboard works across groups; server badges render.

dod|criteria
D1|Slash menu dynamically includes MCP tools
D2|Tools grouped by server with colored badges
D3|Placeholder input schema on select
D4|All tests pass

anti|description
A1|Duplicating the slash menu component — extend existing
A2|Not caching MCP tools (change rarely)

---

### sec-CHAT-018 — Personal Knowledge Base (Client‑Side RAG)
effort|pri|deps|$cut
14.0|H|CHAT-001,CHAT-002,FND-004,FND-005|$lc:sandbox,$lc:knowquota,$g15,$g17

sub|type|description
CHAT-018A|pipe|Install `@xenova/transformers`, `client-vector-search`, `localforage`, `dompurify`.
CHAT-018B|pipe|Create `textChunker.ts`: `chunkDocument(text,options)`, recursive split, 512 chars, 64 overlap, preserve metadata.
CHAT-018C|pipe|Create `embeddingEngine.ts`: lazy‑load `Xenova/all-MiniLM-L6-v2` via `pipeline('feature-extraction')`, cache model in IndexedDB, `generateEmbedding()` / `generateEmbeddings()`.
CHAT-018D|pipe|Create `vectorStore.ts`: wrap `client-vector-search` `EmbeddingIndex`; `addDocument, search, removeDocument, clearAll`. Persist index to IndexedDB.
CHAT-018E|pipe|Create `knowledgeStore.ts` (Zustand): `documents, isIndexing, indexingProgress, isSearching, lastSearchResults, storageEstimate`.
CHAT-018F|pipe|Create `useKnowledgeBase` hook: `uploadFiles, search, deleteDocument, getContextForPrompt`. Monitor storage quota.
CHAT-018G|pipe|`FileUploadZone`: dropzone for txt, md, pdf, csv, json, html; queue sequential processing; progress steps.
CHAT-018H|pipe|`DocumentList`: list files with metadata; delete; empty state.
CHAT-018I|pipe|`KnowledgeBasePanel`: tabs Documents/Upload/Search Test; stats bar; "Clear" danger button; auto‑inject toggle.
CHAT-018J|pipe|Wire into Chat: optionally inject relevant context on send; toggle per conversation; "📚 Searching..." indicator.
CHAT-018K|behavioral|[TEST] File upload chunks/embeds; search returns relevant; delete removes from store; quota warns at 80%; context injection toggle.

dod|criteria
D1|Upload → chunk → embed → store in IndexedDB
D2|Semantic search with cosine similarity (~88ms/10K docs)
D3|Context injection with per‑conversation toggle
D4|Storage quota monitoring and warnings
D5|All processing client‑side
D6|All tests pass

anti|description
A1|Embedding on main thread — verify Web Worker usage
A2|Not monitoring quota — silent write failures
A3|Embedding every keystroke — debounce search
A4|Injecting all knowledge into every conversation — must be opt‑in

---

### sec-CHAT-019 — Memory Manager (Cross‑Conversation Context)
effort|pri|deps|$cut
5.0|M|CHAT-018,CHAT-002|$lc:knowquota

sub|type|description
CHAT-019A|pipe|Create `memoryStore.ts`: `entries[], isSummarizing, autoSummarize, retentionDays`.
CHAT-019B|pipe|Create `useMemory` hook: `summarizeConversation(threadId)`, `getRelevantMemories(query)`, manual CRUD. Auto‑summarize on conversation end (≥5 messages). Store in IndexedDB.
CHAT-019C|pipe|`MemoryPanel`: auto‑summarize toggle; retention slider (7/30/90/365 days); search; "Add Memory"; "Clear All" danger button.
CHAT-019D|pipe|`MemoryEntryCard`: text preview, source, tags, edit/delete.
CHAT-019E|pipe|Wire into Chat: inject relevant memories as system context on new conversation; toggle; "🧠 N memories loaded" badge.
CHAT-019F|behavioral|[TEST] Summarization creates memories; relevant memories retrieved; manual CRUD; retention deletes old; injection toggle.

dod|criteria
D1|Auto‑summarization with message threshold
D2|Manual memory CRUD; semantic search retrieval
D3|Memory injection into new conversations with toggle
D4|Retention policy with configurable duration
D5|All data client‑side IndexedDB
D6|All tests pass

anti|description
A1|Auto‑injecting without user awareness
A2|Summarizing tiny conversations (<5 messages)
A3|Infinite memory growth — retention policy mandatory

---

### sec-CHAT-020 — Agent Studio (Custom Agent Builder)
effort|pri|deps|$cut
6.0|M|DASH-006,CHAT-018|—

sub|type|description
CHAT-020A|pipe|Extend `agentStore.ts` with custom agents: `{ id, name, avatar, systemPrompt, knowledgeBaseIds, mcpServerIds, model, isBuiltIn, createdAt, updatedAt }`.
CHAT-020B|pipe|`AgentStudio`: route `/agents/studio` or modal; sidebar list of custom agents; "New Agent" button; right content area form.
CHAT-020C|pipe|`AgentBuilderForm`: name, avatar, system prompt (textarea), model select, knowledge base multi‑select, MCP tools multi‑select. react‑hook‑form + zod. Save/Delete actions.
CHAT-020D|pipe|`AgentTestChat`: mini chat in studio to test agent behavior with its config.
CHAT-020E|pipe|Wire into AgentFleetPanel: custom agents appear alongside built‑in; edit and duplicate (for built‑ins) buttons.
CHAT-020F|behavioral|[TEST] AgentStudio opens; form saves new agent; test chat responds; custom agent in fleet; duplicate creates editable copy; delete only custom.

dod|criteria
D1|Agent builder with system prompt, knowledge, tools
D2|Test chat for validation
D3|Custom agents in fleet; duplicate built‑ins
D4|All data persisted in Zustand
D5|All tests pass

anti|description
A1|Storing agent configs without validation (empty prompt)
A2|Not showing attached resources on card
A3|Allowing deletion of built‑in agents

---

### sec-CHAT-021 — Agent‑to‑Agent Flow Orchestrator
effort|pri|deps|$cut
14.0|H|CHAT-020,DASH-006|$lc:a2anode

sub|type|description
CHAT-021A|pipe|Install `@xyflow/react`, `dagre`, `@types/dagre`.
CHAT-021B|pipe|Create `flowStore.ts`: `nodes, edges, isRunning, runProgress`. Actions: `addNode, removeNode, addEdge, removeEdge, autoLayout, startRun, updateProgress, endRun`. Apply `zeitstand` undo/redo.
CHAT-021C|pipe|Build `AgentNode.tsx`: custom React Flow node with avatar, name, model badge, input/output `<Handle>`, status indicator (idle/running/completed/error), click opens AgentStudio.
CHAT-021D|behavioral|Build `A2AFlowEditor.tsx`: `<ReactFlow>` with `nodeTypes`, `defaultEdgeOptions` (electric blue, animated), Background dot grid, Controls, MiniMap. Drag from AgentFleetPanel sidebar to canvas. Connect handles to create edges.
CHAT-021E|pipe|`FlowToolbar`: "Auto Layout" (Dagre, `rankdir:'LR'`), Undo, Redo, Fit View, Clear, flow name, save indicator, "Run Flow" button.
CHAT-021F|pipe|`FlowRunPanel`: slide‑in from bottom; shows current executing agent, progress, agent output preview, Stop, "View Results".
CHAT-021G|pipe|Wire: "Create Flow" button in AgentFleetPanel; saved flows in fleet; trigger from Chat (`/run_flow`).
CHAT-021H|behavioral|[TEST] Nodes draggable; edges connectable; auto‑layout hierarchical; undo/redo works; flow run highlights sequentially; stop aborts; `/run_flow` triggers.

dod|criteria
D1|Visual DAG editor with drag‑and‑drop
D2|Dagre auto‑layout; undo/redo
D3|Flow execution with progress visualization
D4|Triggering from slash commands and fleet
D5|All tests pass

anti|description
A1|Using HTML5 drag‑and‑drop — React Flow handles this
A2|Forgetting `<Handle>` on custom nodes
A3|Not cleaning Dagre graph on node delete
A4|Running flows without visual feedback

---

### sec-CHAT-022 — Prompt Library
effort|pri|deps|$cut
3.0|M|CHAT-006,CHAT-008|—

sub|type|description
CHAT-022A|pipe|Create `promptStore.ts`: `prompts[], categories[], selectedCategory, searchQuery, isOpen`. Actions: `addPrompt, updatePrompt, deletePrompt, setCategory, setSearchQuery, openLibrary, closeLibrary, incrementUsage`. Persist to localStorage.
CHAT-022B|pipe|Create `mockData/prompts.ts` with 8‑10 default prompts across categories (coding, writing, analysis…).
CHAT-022C|pipe|`PromptLibraryPage`: route `/prompts`; left category sidebar, right grid; search; "New Prompt" button.
CHAT-022D|pipe|`PromptCard`: title, preview, category/tag badges, usage count. Hover lift, click edit modal.
CHAT-022E|pipe|`PromptLibraryModal`: create/edit with shadcn `Dialog`; title, content textarea, category select, tags input. zod validation.
CHAT-022F|pipe|Integrate with ChatInput: "Prompt Library" button in toolbar; `Cmd/Ctrl+P` opens quick‑insert menu; dropdown above input; click inserts.
CHAT-022G|pipe|Slash command integration: add `/prompt` command.
CHAT-022H|pipe|Usage tracking: increment on insert, sort by "Most Used".
CHAT-022I|behavioral|[TEST] Page renders; CRUD works; quick‑insert menu opens on button and shortcut; `/prompt` slash; usage increments.

dod|criteria
D1|Prompt library page with categories, grid, search
D2|CRUD with localStorage persistence
D3|Quick‑insert from ChatInput via button and `Cmd+P`
D4|`/prompt` slash command
D5|Usage tracking and "Most Used" sorting
D6|All tests pass

anti|description
A1|Not persisting — prompts lost on refresh
A2|Using path params for editing — causes remount
A3|Forgetting `Cmd/Ctrl+P` shortcut
A4|No validation on prompt content

════════════════════════════════════════════════════════════
## END OF 11-Chat.txt
## Checkpoint: CHAT-001 through CHAT-022 ready for execution.
## Global contracts ($g, $c, $lc) inherited and extended.
## Paste the CHECKPOINT BLOCK at top into any new session.
## For any task, first load Global Contract block, then navigate to sec‑CHAT‑XXX.
════════════════════════════════════════════════════════════
```